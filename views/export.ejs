<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Export Tax Report</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles/styles.css">
</head>

<body>
  <!-- Navbar -->
  <%- include('partials/navbar') %>

  <!-- ✅ Flash messages -->
  <%- include('partials/flash') %>

  <div class="container mt-5" style="max-width: 500px;">
    <div class="card shadow-sm">
      <div class="card-body text-center">
        <h5 class="card-title mb-3">Generiere Steuerbericht</h5>
        <p class="text-muted">
          Wähle das Steuerjahr aus für den PDF Export an das Finanzamt.
        </p>

        <form id="exportForm" onsubmit="handleExport(event)">
          <div class="mb-3">
            <label for="year" class="form-label">Auswahl</label>
            <select id="year" name="year" class="form-select" required>
              <% 
                const currentYear = new Date().getFullYear();
                for (let y = currentYear; y >= 2020; y--) { 
              %>
                <option value="<%= y %>"><%= y %></option>
              <% } %>
            </select>
          </div>

          <button type="submit" class="btn btn-primary w-100">
            <i class="bi bi-file-earmark-pdf"></i> PDF Download
          </button>
        </form>

        <hr class="my-4">

        <small class="text-muted">
          Der Bericht berücksichtigt nur Verkäufe aus dem ausgewählten Jahr für den zu versteuernden Gewinn und Käufe nach FIFO Prinzip, die nicht länger als ein Jahr zurückliegen.
        </small>
      </div>
    </div>
  </div>

  <footer class="bg-dark text-white text-center py-3 mt-5">
    <p>&copy; 2025 Crypto Tax Reporter. All rights reserved.</p>
  </footer>

  <script>
    function handleExport(event) {
      event.preventDefault();
      const year = document.getElementById("year").value;
      window.location.href = `/export/tax/${year}`;
    }
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
